{
  "name": "engine",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch ./src/index.ts",
    "build": "tsc --noEmit",
    "check-types": "tsc --noEmit",
    "start": "bun ./src/index.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@repo/config": "workspace:*",
    "@repo/domain": "workspace:*",
    "@repo/events": "workspace:*",
    "@repo/logger": "workspace:*",
    "@repo/redis-streams": "workspace:*",
    "redis": "^6.0.1"
  }
}
